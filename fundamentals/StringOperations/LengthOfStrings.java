public  class LengthOfStrings {
    public static void main(String[] args) {
        /**
         * The following program finds the length of a given string
         */
        String hello = "Hello world";
        System.out.println("The length of " + hello + " is " + hello.length());

    }
}
